/**
 * Sentry configuration for Next.js client-side error tracking.
 * 
 * This file is a stub that doesn't import Sentry directly.
 * To enable Sentry:
 * 1. Run: npm install @sentry/nextjs
 * 2. Run: npx @sentry/wizard@latest -i nextjs
 * 3. Set NEXT_PUBLIC_SENTRY_DSN environment variable
 * 
 * The Sentry wizard will create proper sentry.client.config.ts and
 * sentry.server.config.ts files with the correct configuration.
 */

// This file intentionally does nothing when Sentry is not installed.
// The actual Sentry initialization should be done via the Sentry wizard
// which creates the proper Next.js instrumentation files.

export { };
